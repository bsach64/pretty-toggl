```
$ go build
$ ./pretty-toggl help
A CLI app for toggl track!

Usage:
  pretty-toggl [command]

Available Commands:
  auth        Authenticate with an API token!
  completion  Generate the autocompletion script for the specified shell
  current     Displays the current running timer
  help        Help about any command
  list        Get list of all projects, tags, workspaces
  start       Starts a timer
  stop        Stops the current time entry!

Flags:
  -h, --help   help for pretty-toggl

Use "pretty-toggl [command] --help" for more information about a command.
```
